# FanCtl 4.1 计划（v1，审查修复轮之后的裁决与验收轮）

> 状态：草案 v1，待对抗审查。方法循环：计划 → 对抗审查 → 修订 → 做 → 再审。
> 受众不变（作者本人，GitHub=备份），目的函数不变：
> min(过冲、磨损、能耗、热误差) max(泛化、学习地图质量、可用性)。

## 0. 第一性原理：4.1 的价值锚点

4.0 把能力建出来了（校准、采信域、FNum 自愈、升级链路、三套数据仪表），
但按"存在 ≠ 可信"清点，欠账全部是**证据债**而非功能债：

| # | 欠账 | 性质 | 实测现状（2026-09-11 本机） |
|---|------|------|------------------------------|
| 1 | D 秒基改造与否——预注册裁决无裁决资格 | 控制律决策 | 账本受控时长仅 0.83 天；**P 校准线跨桶不等（fast/slow=6.61）**，按规则须"先修账本再裁决" |
| 2 | 包络健康度观察协议（2-3 周收敛判据）到期未裁决 | 数据修正 | gap 1.43→**0.326**，15 桶 1348 样本，趋势自愈 |
| 3 | 负载锚定评估承诺（功耗直方图满 30 天） | 曲线优化 | 历史战报 30 天满，从未召开评估 |
| 4 | B4a 自升级链路 v3.9.0 建成至今**从未真机验收** | 发行链 | 4.0.0 未打 Release，App 内升级无从触发 |
| 5 | B2 校准只在仿真跑过，真机学习表成熟后路径永不触发 | 迁移韧性 | B2 立项理由（换机/清数据行为悬崖）零真机证据 |

结论：4.1 不加新特性。4.1 的主题是**裁决与验收**——把"建好的"变成"被裁决过/被验收过的"。
每一批都直接落在目的函数上：min(过冲/热误差) ← D 裁决；max(学习地图质量) ← 包络收口 +
冷启动验证；max(可用性) ← 升级链路验收。

## 1. 批次

### 4.1-A dt 裁决前置审计（立即，分析 + 数据基建小修）
**A1 账本生命周期缺陷（4.1 的第一个真发现）**：dt 账本挂在 `AIControlMetrics` 整体
重置上——`aiMetricsUserTarget != userTarget` 即全新实例 → **用户每次切换目标档位
（72/76/80）账本清零陪葬**。本机 userTarget 现为 72（曾为 76），账本 0.83 天与
09-08 部署以来的墙钟差即为重置痕迹。结构性后果：按正常使用习惯"账本 ≥7 天"门槛
可能**永远不可达**。修法 = 账本从评测指标中拆出：独立重置判据（仅控制律版本变更时
清零），或独立持久化键。不改任何控制律数值。
**A2 口径定版**：预注册规则"账本 ≥7 天"的"天"从未定义（受控时长 vs 墙钟）。实测
受控占比 ~30% → 两种口径的裁决日期差 3 周。修订进 EVOLUTION（先于新数据，防事后
合理化）。
**A3 P 校准线归因预注册**：P 基线跨桶不等有两个假说——(a) 账本 bug（P 归因错桶）；
(b) 快拍选择偏差（快拍由温度急变触发，|Δerror|/秒 天然更高 → P 每秒贡献高是物理）。
先写判据再看数据：用账本同拍字段检验 P/秒 与温度变化率的相关性；若 (b) 成立，则
"D 快/标称比"也含同样偏差，裁决基准须改为"同温度变化率分层的桶内对比"。
**DoD**：A1 有独立重置判据断言（档位切换不清账本）；A2/A3 修订文字入 EVOLUTION。
**回滚**：A1 独立提交，revert 即回现状。

### 4.1-B 包络收口裁决（9 月下旬，观察协议到期）
判据（沿用 v3.6 观察协议）：gap 持续 →0 则**确认自愈、关闭议题**；若不收敛 →
一次性数据修正（学习数据手术，非控制律）。当前 0.326 趋势向好，大概率前者。
**联动**：若走数据手术 → 清桶即触发 B2 校准真机首跑（见 D），一次动作两份证据。

### 4.1-C 负载锚定评估（数据已就绪，随时可开）
功耗直方图 30 天满。按优化器负载锚定的既定口径评估：温度分位下移而功耗分位未变
= 自降温 → 禁止锚点跟进。产出 = 评估结论记账（做或不做曲线修正，数据说话）。

### 4.1-D 真机冷启动 dogfood（A 完成后择机，需一个低负载窗口）
**这是 B2 立项理由的首次真机验证**：备份 learn.json → App 重置学习数据 → 观察
B2 校准全生命周期真机首跑（观察期 auto 采样 → 2 桶/12 样本接管）→ 对照判据。
**预注册判据（先于实验写下）**：接管后过冲不劣于 4.0 HIL 基线容差（+1.5°）、带内
时长不低于 95%；校准日志链完整（校准中→采样→完成/超时→接管各一次）。
**附带观察项**：fan0/fan1 转速差实测（4.0 审查遗留的采样器 fan0-only 偏差，本实验
顺带取数，不单独立项）。
**裁决**：通过 → 保留重学表（学习数据本来就该重新积累）；不通过 → 恢复备份 + 缺陷
回灌修复。**备份文件保留到 4.1-F 之后**。

### 4.1-E D 秒基改造（条件批次：仅当 A 修订后的裁决触发）
控制律变更 = 4.1 内最高风险项。三重验证门槛（预注册规则原文）：VirtualMachine 验
3s 拍严格等价 + HIL 冷启动对比 + 81 族扫描零劣化。**禁止在账本数据不达标时动**。

### 4.1-F 发版 + B4a 首验收（收官）
tag 4.1.0 → Release（CI 资产）→ **真机 App 内点一次升级**（v3.9 建成以来首次真实
使用）：下载→校验→授权→watcher 重启全链路。任何 P1 → 修复回灌 4.1.1，验收记录
入 EVOLUTION。**此批次是 4.1 存在的理由之一：没有发版，B4a 永远是欠账。**

## 2. 明确不做（v1）

继承 4.0 v3 全部 + 新增：
- **无新特性**（N=1 无价值锚点：新 UI/新平台/新模式全部砍除）
- 多风扇学习语义重构（降为 D 的观察项，数据说话后再议）
- lookupBandWidth 敏感性扫描（B3 残余，收益/成本比不足，除非 D 实验打脸采信域）
- 学习数据导出/导入工具（继续违反"移植即污染"）

## 3. 时序

```
9-11   4.1-A 审计与账本修复（A1 代码 + A2/A3 规则修订）
9-下旬 4.1-B 包络裁决（协议到期）＋ 4.1-C 负载锚定评估
A 后   4.1-D 真机冷启动 dogfood（择低负载日，45min+ 窗口）
条件   4.1-E D 秒基（裁决触发才做；触发则 +1 验证周）
收官   4.1-F tag/Release/B4a 验收（预计 10 月上中旬；E 触发则顺延）
```

## 4. 计划审查记录

- v1 五轴自审：
  - **范围**：对抗问题"全是收尾算不算不作为"——反驳：N=1 下新特性无价值锚点；
    可交付定义 = 四个数字有结论（D 比值裁决、gap 收口、负载锚定、升级验收）。
  - **验证**：A1 独立断言；D 预注册判据；E 三重门槛；F 真点一次。
  - **Goodhart**：A2/A3/D 的判据全部先于对应数据写下；冷启动 dogfood 禁止看完
    数据再定"成功"。
  - **时序**：E 依赖 A；F 依赖 D/E 落定；B/C 不阻塞主线。
  - **回滚**：D = learn.json 备份；E = 4.0.0 回滚点；F 失败 = install.sh 手动兜底。
- v1 遗留风险（诚实记账）：
  - D 实验期间机器控制质量回退半天——受众即作者本人，自担；须选无重要负载窗口。
  - B4a 验收可能挖出升级链路 P1（从未真实使用过）——这正是验收的目的，不是风险
    而是 4.1-F 的预期产出之一。
  - A1 若证实账本需独立持久化，涉及 ai-metrics.json 结构变更——decodeIfPresent
    兼容惯例照旧。
