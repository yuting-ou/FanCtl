# 安全政策

FanCtl（清风）包含一个以 **root 权限运行** 的守护进程（fanctld），它直接控制 macOS 的 SMC 风扇。安全影响面大于普通应用，我们严肃对待任何安全问题。

## 支持范围

| 版本 | 支持 |
|---|---|
| 最新 release（当前 v3.1.0） | ✅ 安全修复 |
| 更早版本 | ❌ 仅建议升级 |

## 报告的漏洞类型

- 守护进程提权或越权（安装目录组权限、LaunchDaemon plist 完整性）
- 文件权限问题导致任意本地用户可篡改配置/学习数据/状态文件
- SMC 写入被滥用（如让风扇失效导致硬件过热）
- 配置/状态 JSON 解析导致的崩溃或注入
- 其他任何可被利用的缺陷

## 如何报告

请**不要**将安全漏洞公开发布在 Issue 或 PR 中（仓库是公开的）。

- 优先使用 GitHub 的 **Security Advisories** → **Report a vulnerability**（私有报告通道）
- 或在 Issue 中仅以模糊描述说明"存在安全问题"，我们再私密沟通

我们会在确认后尽快响应。修复发布前，请勿公开漏洞细节（负责披露，通常 90 天协调期）。

## 安全设计摘要

- App 无特权、不直连 SMC；daemon 是唯一 SMC 写者
- 传感器故障 / 写入失败 → 交还系统调度，绝不把风扇钉死在强制值
- 92°C 高温兜底与 SSD 托底永远压过用户意图
- 配置损坏自动备份回写默认
- 全链路 NaN/Inf 防御，SMC 键值钳位防崩溃
