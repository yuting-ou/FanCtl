# 清风 · 发行说明

> CI 的 release job 会把本文件正文作为 GitHub Release 的说明文字（`--notes-file`）。
> 为什么不用 tag 批注：`actions/checkout` 检出 tag 时建的是 **lightweight** 引用，
> `git tag -l --format='%(contents)'` 会退化成 commit message（v4.2.1 实测如此），
> 迁移警示就丢了。放在仓库里还能被门禁静态检查（版本号与警示必须在场）。

## 安装 / 升级

1. 下载本 Release 的 `FanCtl-vX.Y.Z.zip` 并解压；
2. 在解压目录执行 `sudo ./install.sh`（需要密码：安装守护进程与菜单栏 App）；
3. 菜单栏出现「清风」即完成；建议在面板菜单里打开「登录时启动」。

## 从旧版升级要注意的

- **4.2.x 起，特权脚本住在 root 拥有路径**：`/usr/local/libexec/fanctl-upgrade.sh`
  与 `fanctl-uninstall.sh`。从 4.1.x 升上来需要**先手动 `sudo ./install.sh` 一次**
  （旧版 App 的升级脚本没有安装这两个文件的逻辑）；此后 App 内一键升级会自我刷新它们。
- **4.2.0 的 App 内一键升级不可用**（目录信任门有个"函数定义晚于调用"的缺陷，升级会
  稳定失败，不影响已装功能）。装了 4.2.0 请直接用 `sudo ./install.sh` 升到 4.2.1+。
- **卸载**：`sudo /usr/local/libexec/fanctl-uninstall.sh`（4.2.0 之前的机器改用
  `sudo "/Applications/清风.app/Contents/Resources/uninstall.sh"`）。
- **出问题请先跑诊断包**：`swift run -c release --disable-sandbox fanprobe --report`
  或发行包外的 `fanprobe --report`，把整段输出贴进 issue（无需 root，只读）。

## 本版变更要点

- 4.2.17：`fanprobe` 的 14 日磨损趋势**加了第二列并改了口径**：`2026-09-17: 12.78 · 8%（调速 1519 次）`
  ——左边还是 `次/采样分`，右边是**墙钟覆盖率**（当日计入分母的采样秒 ÷ 86400），它回答"左列那份速率能信几成"。
  本机两周实数据：覆盖率 68% → 8~19%，同期采样口径从 3.21 涨到 16.42（看着像磨损在恶化），而每日真实调速次数
  从 6243 掉到 2900 量级。顺带把上一轮那句"偏高"的量级校准了：用功耗直方图当"daemon 真跑了多久"的尺去分解，
  覆盖率下降里约 **92–100% 是整机睡眠/daemon 没跑**，只有 0–11% 是温度门拒收——所以两个口径不是"谁真谁假"，
  而是分别回答"忙时强度"和"每日历分钟摊多少"。分母不足 30 秒的日子不再整行消失（左列印 `—`，右列给出原因）；
  时钟回拨/手改 history 造出的"未来日"不给覆盖率。趋势行文本搬成有断言的纯函数（此前全仓没有一条测试读过
  fanprobe 的输出）。另新增"断言数契约门槛两源同值"的门（此前漏改 CI 下限是静默的）。诊断包 `--report` 的
  19 行模板不变。测试 5006 → 5016 断言 / 91 组，门槛双源 4995 → 5005。
- 4.2.16：**磨损速率不再是只有一把有偏的尺**。旧口径 `次/采样分` 的分子（调速次数）在温度读数失真的拍
  也计数，分母（采样秒）却不把那些拍算进去 ⇒ 速率被系统性抬高，而 R45 当时只改了标签、没给出抬高多少。
  现在 `fanprobe --report` 的磨损行与 `fanprobe` 的人读表**并排给出当日墙钟口径**，本机 2026-09-26 实测
  `速率 17.04 次/采样分 · 墙钟 6.79 次/墙钟分`（差 2.51 倍）。两个口径各偏一边（采样侧漏掉没采样的时间 ⇒ 偏高，
  墙钟侧把整机睡眠/daemon 停着的时间也算进去 ⇒ 偏低），所以文案给的是"真值落在两者之间"，不再让你只能选一个数信。
  拿不到当日墙钟（归档日、或当日不足 30 秒）时印 `—` 而不是 0；安静日不再印 `nan 倍`（倍数改写成
  "墙钟分钟 ÷ 采样分钟"，分子约掉之后不可能出现除零）。趋势落盘节奏从裸字面量提成命名常量并被静态门钉住
  （改回"每拍写盘"现在会红）。测试 4977 → **5006 断言 / 91 组**，契约门槛双源 4970 → **4995**。
- 4.2.15：面板每拍渲染成本的**归因**做完了：给 `--tickbench` 加了 `--vary temp|rpm|pct|power`（只让一类量每拍抖动，其余钉住）
  与 `--static`（什么都不抖）两个判据，于是能问"到底是谁在花钱"。结果：只抖功耗 867ms/拍 → 去掉头部功耗胶囊的
  数字转场与每拍补间后 **6ms/拍**；只抖转速 164ms/拍 → 去掉能量条补间后 **19ms/拍**；什么都不抖 5–16ms/拍（这三组都是**离屏、同一配置内**的 A/B 差值，不是上屏绝对值：本量具的绝对值在会话之间能差 2 倍，见 EVOLUTION R52）。
  据此撤掉面板里 6 处"每拍抖动的量"的转场/补间（温度主读数、GPU/部件温度、两条条形、功耗胶囊），并把趋势环
  改成按它自己的分辨率（200 点 / 600s = 3s/点）采样、只在真的多了一个点时才重发 history（34 拍从 34 次更新降到 10 次）。
  **诚实的结论**：真机面板开着、所有量都在抖时，每拍中位数**没有下降**（上屏 847/847 → 856/847ms），只有均值降了
  约 15%（860→727ms）——因为只要温度在变，就还有一整笔与动画无关的重排在等着（火焰图已从字形栅格移到
  AppKit display-cycle 的 `layoutIfNeeded`）。这一笔留给下一轮。测试 4973 → 4977 断言 / 90 组。
- 4.2.14：面板"每拍渲染成本"第一次有了**量具**：`FanCtl --tickbench`（离屏真窗口托管真实面板、
  用真机 status.json 当夹具逐拍抖动、写盘全部落临时目录），打印每拍 CPU 微秒数与内存斜率。
  用它把上一轮停放的问题判完：**撤掉温度卡级补间与 `assign()` 的事务级动画换不来任何成本**
  （留着 804/818/834ms/拍 vs 撤掉 848/867ms/拍，差在噪声内；把玻璃换成实心材质、给主温度去掉
  渐变与辉光同样不动）——所以这两处观感**保持原样**，并把"别再为了省 CPU 撤它"写进失败账本。
  同时量出一件更大的事：**面板开着时每一拍数据要烧掉数百毫秒 CPU**（面板关闭时 0.2ms），
  火焰图指向 CoreAnimation 事务刷新里的**字形从轮廓重新栅格化**（渐变/辉光/阴影文字走的慢路径），
  与动画无关——这是下一轮的头号目标。UI 动画静态门补上三条可绕路径（按数据源判、前缀匹配、
  转场与 `withAnimation` 逐文件死预算），并新增「渲染量具安全门(R51)」钉住量具只读真夹具、
  写盘只能落临时目录。测试 4947 → 4973 断言 / 89 → 90 组；两路独立审查抓到 9 条，全修（详见 EVOLUTION R51 审查三分类）。
- 4.2.13：续 4.2.12 的动画收口——**逐项功耗百分比**（面板"功耗分项"里的 CPU/GPU 占比）
  原先也挂着 numericText 数字转场 + 补间，而它是每拍抖动的量（与风扇转速同族），
  现在改为等宽直读。其余保留的数字转场都逐个核过驱动源：温度、有效目标、经验百分比等
  只在**整数位真的变了**时才滚一下，不属于同一缺陷。UI 动画静态门同时扩了一条，
  钉住"每拍抖动的百分比不挂转场"，防止以后又被加回去。
- 4.2.12：**面板里"风扇那一行每拍自己滚一下/缩一下"的动画治掉了**（作者指出后从源码定位，
  非猜测）。根因两处：① `FanModel` 把**每拍的转速赋值**整个包在 `withAnimation(.snappy(0.25))`
  里——事务级动画会把该子树里同一事务中变化的所有可动画量一起补间；② `FanRow` 的转速与百分比
  两个数字用了 `.contentTransition(.numericText())`，而转速每 1–3s 就随传感器抖动换值，
  数字转场带缩放/滚动，观感即"自己在缩小"（高频 numericText 在本项目还有字形位图内存膨胀的前科）。
  现在：赋值不再带动画事务，两个每拍数字改为等宽直读（`monospacedDigit` 保证布局不抖），
  能量条的平滑动画保留（那是真正的连续信号）。另修风扇图标尺寸被两处各写一套（内部 24×24、
  调用方又给 22 宽）的矛盾，收成单一常量 `fanSpinnerSide`。新增 UI 动画静态门（源码级，
  读不到源码判红）钉住这三条例子，防止回归。温度卡等其他数字转场不动。
- 4.2.11：4.2.10 的两处修正（同一轮独立审查抓到）。① 新加进摘要的两个目标值改为**按 5%
  粗量化**：`baseTargetPercent` 是覆盖前的连续插值、每拍都写，逐整数纳入会让"安全托底把输出
  钉住"的那些拍每拍跨整数 → 状态落盘频率涨到每拍一次，与本轮豁免 `loopInterval`/`thermalModel`
  的理由自相矛盾；5% 粒度下守卫接入/解除一定翻段、连续微漂不翻。② 变化感知门补上结构性盲区：
  它原先按**夹具里出现的 JSON 键**枚举，两份夹具都留 nil 的字段被编码器省略、根本不进差分——
  现在用 Mirror 枚举存储属性，每个字段要么被差分、要么在豁免名单、要么显式承认"夹具未填"。
- 4.2.10：**安全托底目标不再最长陈旧 10 秒**，并给"每个 status 字段都必须被变化感知看见"
  装了第一道通用门。`baseTargetPercent`（覆盖前的基础目标）与 `safetyFloorPercent`
  （SSD/高温安全托底目标）此前**不在** `statusChangeSummary` 里：它们翻转的那一拍若恰好
  没有别的字段同时变化，状态就不落盘，App 的「安全托底」胶囊最长陈旧一个 10s 心跳。
  新增的结构门会对 status.json 的**每个键**做差分（A→B 逐键替换、重新解码、摘要必须变），
  不参与摘要的键必须写在豁免名单里并附机制理由（现有 11 条：心跳、派生别名、
  每拍漂移量纳入会打穿写盘节流等）。以后新增字段忘了进摘要 = 测试直接红。
- 4.2.9：修**磨损速率的口径标签说谎**。`speedChangesPerMinute` 的分母是**采样秒**
  （`tempSeconds`，每个有效温度样本累加），而 `fanprobe` 的今日战报与趋势块一直印
  「次/受控分」，诊断包另一侧印「次/采样分」——同一个数两种口径说法，而它正是
  「批次 B 控制律要不要动」的裁决读数。现在单位串收成单一真值
  `DailyStats.wearRateUnit = "次/采样分"`，两个 surface 都从它取，并加静态接线门
  （谁再硬编码「次/受控」或另抄一份字面量，测试直接红）。**数值与判据不变，只是停止说谎**。
- 4.2.8：**多风扇机器上"一把风扇读不到"不再被静默钉住**。此前若某把风扇的转速/量程键
  读失败（固件返回 NaN 或键异常），它会被跳过写入——于是那把风扇永远停在最后一次
  强制目标上（`F{n}Md=1` + 过期 `Tg`），系统调度对它失效，而另一把照常受控：状态里只列
  读得到的一把，看上去就像一台单风扇机器。现在**连续失联满 6 秒**即交还系统调度（去抖是
  必需的：间歇 NaN 的键在可见拍上照常受控，单拍即交还会把模式位抖成"交还↔夺回"振荡），
  恢复可见当拍重新接管、再失联再交还、唤醒后仍失联会补交还，daemon 日志出声（交还写失败
  也出声、不逐拍重试）。诊断包"风扇"小节补报现象 `另有 N 把本拍无读数`（只报现象：处置
  判据在引擎那一拍，status 快照没资格下结论）。健康机器（含单风扇）行为零变化。
- 4.2.7：独立审查轮对 4.2.5/4.2.6 两个守卫的补强（两条真洞 + 四道"看着有牙其实是假绿"的门）。
  ① 卸载的删除前缀守卫可被 `/.`、`//`、单层 `/Users` 绕过（实测会拼出 `///Library/Caches/…` 这类
  指向文件系统根的路径），现在先 `cd -P` 归一化，再要求"至少两层且剥掉所有斜杠后仍非空"。
  ② App 落点谓词对**带尾斜杠的符号链接**说"可用"（`test -L path/` 会跟随链接），现在先剥尾斜杠，
  并把 `/` 与空串判为拒绝。③ 门禁自身：预检的匹配锚到行首 `if`（防止"注释掉还算接线"）、复验除文案
  外还要求 `if [[ -L …]]` 判定行还在（防止被 `if false` 掏空）、"禁止 rm -rf 尾斜杠"的扫描不再要求
  行尾、git 里查不到脚本一律判红（原来静默跳过）。用户侧行为不变。
- 4.2.6：修**发行包里给用户看的命令路径**。4.2.5 的 zip 解出来后 `./install.sh`（非 root 时）回显
  `请用 sudo 运行: sudo ./scripts/install.sh`，而 zip 的布局是脚本与 `FanCtl.app` 同级、**根本没有
  `scripts/` 目录**——照着提示敲就必然"没有那个文件"。R33 把产物根改成自适应布局时只改了查找逻辑，
  漏了这些字符串。五处提示统一按调用路径（`$0`）回显，仓库与 zip 两种布局都成立；并加静态门禁止
  回显里再出现 `scripts/{install,uninstall,upgrade}.sh`（门的牙用修复前的提交原文证明）。
  同批发行链两道自证：脚本可执行位（工作树 `-x` + git mode `100755`）、zip 内三个 root 脚本必须可执行。
- 4.2.5：收 root 装机/升级时**按路径写文件**的跟随面（结论来自实测，不是文档）。
  `/Applications` 是 `root:admin` 组可写且**无 sticky**——任何本地管理员用户随时能把
  `清风.app` 这个条目换成指向别处的符号链接；而 BSD `cp -R src dst` 会**跟随**这种 dst
  （实测），于是 root 会把一棵 root 属主的 bundle 写进链接指向的目录，随后的 `chown -R`
  （默认不跟随，实测）又不会把它交还给用户——装完还在原地，但 App 其实没落到该落的地方。
  `install.sh`/`upgrade.sh` 现在都在停服务**之前**预检落点（不是真实目录就拒绝，系统原样不动），
  并在 `cp -R` 之后复验；卸载侧补了删除前缀守卫：问不到登录用户家目录时跳过缓存清理，
  绝不拿可能为空的前缀去 `rm -rf`。另把 `cp -R`/`install`/`chown -R`/`xattr -dr`/`rm -rf`
  对符号链接的实际语义钉成回归门（root 脚本门禁 49 → 62 条）。用户侧无需任何额外操作。
- 4.2.4：诊断包里所有**从盘上读来的自由文本**统一消毒。动因：`/Applications/清风.app` 被安装脚本
  chown 给登录用户，属主可往 `CFBundleShortVersionString` 写任何东西（含 `ESC[2J` 这类 ANSI 转义，
  报告粘进终端会被真解释）；`exit-reason.flag` 与 SMC 错误串同理。规则：身份类（版本）限长 64、
  只收可打印 ASCII，可疑值整体替换成固定标记不保留片段；其余文本压平换行、控制字符转空格、超 200
  字符截断并如实报出原长。另修一处自锁死：build.sh 里"回归测试"排在"重生成版本常量"之前，
  于是提了 VERSION 就必然构建失败——顺序已调正。
- 4.2.3：`status.json` 新增 daemon 自报版本（`daemonVersion`），诊断包的"装机"小节从此能
  直接回答"这台机器上跑的 daemon 是哪一版"——此前只能靠 App plist 版本与二进制 mtime 推断，
  遇到"App 已升级、daemon 留在旧版"的半途状态无从判断。旧 daemon（或该值被改/被拒收）不写
  此字段，报告只说"未自报"，不猜版本。解码侧限长 64 且只收可打印 ASCII——这个文件在同组可写目录。
- 4.2.2：诊断包 `fanprobe --report`（一条命令产出陌生人机器上首问的全部信息，issue 模板已
  设为必填）；`fanprobe` 改真只读——以前它以登录用户身份读坏 JSON 时会在 root 的数据目录里
  写 `.corrupted` 备份并顺带轮转删除旧备份，现在这条写路径在诊断工具里彻底关掉；发行说明
  改由本文件提供并加"说明与 tag 同源"硬门。
- 4.2.1：修 4.2.0 的两个 P1——App 内一键升级必然失败（信任门函数顺序）、暂存的 root
  执行脚本正文未纳入哈希复核（同 uid 可在授权窗口内换正文）。
- 4.2.0：批次 A 特权信任根（root 执行代码只住 root 拥有、组不可写路径）；睡醒不再误报
  闭环故障；`history.json` 逐日抢救；AI 评测改按秒加权；配置损坏回 last-good；
  SMC 启动瞬时失败改为进程内退避重试。
